# Optional. Without this key the agent uses deterministic local extraction.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Tuning knobs
REQUEST_TIMEOUT_SECONDS=25
MAX_PAGES_PER_DOMAIN=6
MAX_TEXT_CHARS_PER_PAGE=12000